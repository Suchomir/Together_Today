Utils dir is useless for now
Dist (IS NOW STATIC) is for static photo like main logo, js and probably tailwind

End iinstalation of tailwind

npx tailwindcss -i ..\front-end\css\app.pcss -o .\css\app.css --watch RUN FROM DIST, do not work, something wrong with git


/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["../templates/**/*.{html,js}", "./node_modules/flowbite/**/*.js"],
  theme: {
    extend: {},
  },
  plugins: [require("flowbite/plugin")],
};
